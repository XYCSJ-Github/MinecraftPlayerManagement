<UserControl x:Class="mpm_GUI.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="1120" d:DesignHeight="700">

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="4">
        <StackPanel Margin="6,10,6,16">
            <!-- mpm配置 -->
            <Border Style="{StaticResource CardBorder}" Padding="24,20">
                <StackPanel>
                    <TextBlock Text="mpm配置" FontSize="16" FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimaryBrush}"/>
                    <TextBlock Text="应用会以「后台模式」启动 mpm.exe 并通过共享内存通信。" FontSize="12"
                               Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,14"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="mpm.exe 路径" FontSize="13" Foreground="{StaticResource TextPrimaryBrush}"
                                   VerticalAlignment="Center" Margin="0,0,14,0"/>
                        <TextBox Grid.Column="1" Text="{Binding EnginePath, UpdateSourceTrigger=PropertyChanged}"/>
                        <Button Grid.Column="2" Content="浏览..." Style="{StaticResource FlatButton}"
                                Margin="8,0,0,0" Command="{Binding BrowseEngineCommand}"/>
                        <Button Grid.Column="3" Content="保存" Style="{StaticResource PrimaryButton}"
                                Margin="8,0,0,0" Command="{Binding SaveSettingsCommand}"/>
                    </Grid>

                    <StackPanel Orientation="Horizontal" Margin="0,18,0,0">
                        <TextBlock Text="mpm状态" FontSize="13" Foreground="{StaticResource TextPrimaryBrush}"
                                   VerticalAlignment="Center"/>
                        <Ellipse Margin="12,0,6,0">
                            <Ellipse.Style>
                                <Style TargetType="Ellipse" BasedOn="{StaticResource StatusDot}">
                                    <Setter Property="Fill" Value="{StaticResource TextMutedBrush}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                            <Setter Property="Fill" Value="{StaticResource OkBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Ellipse.Style>
                        </Ellipse>
                        <TextBlock Text="{Binding EngineStatus}" FontSize="13" FontWeight="SemiBold"
                                   Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>

                        <Button Content="启动mpm" Style="{StaticResource FlatButton}" Margin="24,0,8,0"
                                Command="{Binding StartEngineCommand}"/>
                        <Button Content="重启mpm" Style="{StaticResource FlatButton}" Margin="0,0,8,0"
                                Command="{Binding RestartEngineCommand}"/>
                        <Button Content="停止mpm" Style="{StaticResource DangerButton}"
                                Command="{Binding StopEngineCommand}"/>
                        <Button Style="{StaticResource IconButton}" Margin="8,0,0,0" Content="&#xE838;"
                                Command="{Binding OpenEngineFolderCommand}"
                                ToolTip="打开mpm所在目录" FontFamily="Segoe MDL2 Assets"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- 运行日志 -->
            <Border Style="{StaticResource CardBorder}" Padding="24,20" Margin="0,14,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="运行日志" FontSize="15" FontWeight="SemiBold"
                                       Foreground="{StaticResource TextPrimaryBrush}"/>
                            <TextBlock Text="（最近 400 条）" FontSize="12" Margin="10,2,0,0"
                                       Foreground="{StaticResource TextMutedBrush}"/>
                        </StackPanel>
                        <Button Grid.Column="1" Content="清空" Style="{StaticResource FlatButton}"
                                Padding="12,4" FontSize="12" Command="{Binding ClearLogsCommand}"/>
                    </Grid>

                    <Border Grid.Row="1" Background="#1E1E1E" CornerRadius="8" Padding="10,8" MinHeight="220">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding Logs}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}" Foreground="#D4D4D4"
                                                   FontFamily="Consolas" FontSize="12"
                                                   TextWrapping="Wrap"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Border>
                </Grid>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
